# zad 1

# group = df.groupby(['Rok']).agg({'Liczba': 'sum'})
# group.plot()
# plt.legend()
# plt.show()

# zad 3

# group = df.groupby(['Plec']).agg({'Liczba': 'sum'})
# group.plot.pie(subplots=True, autopct='%.2f %%', fontsize=20)
# plt.show()